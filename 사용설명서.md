# sdfdesk 클라이언트 사용 설명서

**버전: 2.1.8**

## 개요

`sdfdesk`는 RustDesk 기반의 CLI(Command Line Interface) 원격 데스크톱 클라이언트입니다. 자동화 테스트와 스크립트를 통한 원격 접속을 위해 설계되었으며, GUI 없이 명령줄에서 직접 실행할 수 있습니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| **포트 포워딩** | 원격 호스트의 포트를 로컬 포트로 연결 |
| **연결 테스트** | 원격 호스트와의 연결 상태 확인 |
| **서버 모드** | sdfdesk를 서버로 실행하여 원격 접속 대기 |
| **Direct 서버** | 클라이언트가 IP로 직접 연결 가능 (포트 21118) |
| **비밀번호 설정** | 영구 비밀번호 설정 및 관리 |
| **피어 목록 조회** | sdfdesk-server에서 온라인 피어 목록 조회 |
| **Electron GUI** | 화면 공유 및 원격 제어 (마우스/키보드) |

---

## CLI 옵션 전체 목록

### 연결 관련

| 옵션 | 설명 |
|------|------|
| `-p, --port-forward <SPEC>` | 포트 포워딩. 형식: `원격ID:로컬포트:원격포트[:원격호스트]` |
| `-c, --connect <ID>` | 연결 테스트 (원격 ID에 연결 시도) |
| `-P, --pwd <PASSWORD>` | **연결 비밀번호** (원격 피어 접속 시 사용) ⭐ |
| `-k, --key <KEY>` | **서버 공개키** (커스텀 ID/Relay 서버용) |
| `--unlock-id <USERNAME>` | OS 원격 잠금 해제 시 사용자명 |
| `--unlock-pw <PASSWORD>` | OS 원격 잠금 해제 시 비밀번호 |

### 서버 모드

| 옵션 | 설명 |
|------|------|
| `-s, --server` | 서버 모드로 시작 (ID 자동 생성) |
| `--direct-server` | **Direct IP 접속 활성화** (포트 21118) ⭐ |
| `--password <PASSWORD>` | 영구 비밀번호 설정 |

### Electron / 화면 공유

| 옵션 | 설명 |
|------|------|
| `--local-server <ID:PORT>` | Electron용 로컬 서버 시작. 형식: `원격ID:WebSocket포트` |
| `--rdp-id <USERNAME>` | Headless 모드 RDP 사용자명 |
| `--rdp-pw <PASSWORD>` | Headless 모드 RDP 비밀번호 |

### 서버 설정

| 옵션 | 설명 |
|------|------|
| `--set-hbbs <HOST>` | 커스텀 Rendezvous 서버 설정 (빈 문자열로 초기화) |
| `--set-hbbr <HOST>` | 커스텀 Relay 서버 설정 (빈 문자열로 초기화) |
| `--set-key <KEY>` | 커스텀 서버 공개키 설정 (빈 문자열로 초기화) |
| `--show-config` | 현재 서버 설정 표시 |

### 조회 및 기타

| 옵션 | 설명 |
|------|------|
| `--list-peers <HOST[:PORT]>` | 온라인 피어 목록 조회 (기본 포트: 21114) |
| `--get-id` | 현재 ID 출력 |
| `--version` | 버전 출력 |
| `--help` | 도움말 |

---

## 사용 예제

### 1. 서버 모드 시작

```bash
# 기본 서버 모드
sdfdesk -s
# 출력: id=XXXXXXXXX

# Direct IP 접속 활성화 (클라이언트가 IP:21118로 직접 연결 가능)
sdfdesk -s --direct-server
```

### 2. Direct 연결 (IP로 직접)

서버가 `--direct-server`로 시작되면 클라이언트는 ID 대신 **IP 주소로 직접 연결**할 수 있습니다.

```bash
# 서버 (호스트 PC)
sdfdesk -s --direct-server
# 출력: id=123456789, Direct access enabled on port 21118

# 클라이언트 (원격 PC) - IP로 직접 연결
sdfdesk -c 192.168.1.100 -P mypassword
```

### 3. 포트 포워딩

```bash
# RDP 포트 포워딩 (ID 기반)
sdfdesk -p 123456789:3389:3389 -P mypassword

# SSH 포트 포워딩 (ID 기반)
sdfdesk -p 123456789:2222:22 -P mypassword

# 커스텀 서버 사용 시 (-k로 서버 공개키 지정)
sdfdesk -p 123456789:3389:3389 -P password -k "서버공개키"
```

### 4. Electron GUI 연동

```bash
# 로컬 서버 시작 (Electron이 ws://localhost:8765에 연결)
sdfdesk --local-server 123456789:8765 -P mypassword

# Headless 모드 (RDP 자동 생성)
sdfdesk --local-server 123456789:8765 -P mypassword --rdp-id admin --rdp-pw adminpw
```

### 5. 피어 목록 조회

```bash
# 서버에서 온라인 피어 목록 조회
sdfdesk --list-peers myserver.com
sdfdesk --list-peers myserver.com:21114

# 출력 예시:
# ID                   IP                   Online
# --------------------------------------------------
# ABC123DEF456         192.168.1.100        ✓
# XYZ789GHI012         10.0.0.50            ✓
```

### 6. 서버 설정

```bash
# 커스텀 서버 설정
sdfdesk --set-hbbs myserver.com
sdfdesk --set-hbbr myserver.com
sdfdesk --set-key "공개키문자열"

# 현재 설정 확인
sdfdesk --show-config

# 기본값으로 초기화
sdfdesk --set-hbbs ""
```

---

## 주요 옵션 구분 ⚠️

| 옵션 | 용도 | 예시 |
|------|------|------|
| `-P, --pwd` | **원격 피어 연결 비밀번호** | `-P mypassword` |
| `-k, --key` | **서버 공개키** (커스텀 서버용) | `-k "공개키"` |
| `--password` | **영구 비밀번호 설정** (서버용) | `--password mypassword` |

> **주의**: `-k`는 서버 키, `-P`는 연결 비밀번호입니다. 혼동하지 마세요!

---

## 사용 시나리오

### 원격 데스크톱 (RDP)

```bash
# 원격 서버
sdfdesk -s
# 출력: id=123456789

# 로컬 클라이언트
sdfdesk -p 123456789:3389:3389 -k password
mstsc /v:localhost:3389
```

### SSH 터널링

```bash
sdfdesk -p 123456789:2222:22 -k password
ssh user@localhost -p 2222
```

### 데이터베이스 접속

```bash
sdfdesk -p 123456789:3306:3306 -k password
mysql -h 127.0.0.1 -P 3306 -u user -p
```

---

## 로그 및 디버깅

```bash
# Windows PowerShell
$env:RUST_LOG="debug"
sdfdesk -s

# Linux/macOS
RUST_LOG=debug sdfdesk -s
```

| 레벨 | 설명 |
|------|------|
| `error` | 에러만 |
| `warn` | 경고 + 에러 |
| `info` | 일반 정보 (기본값) |
| `debug` | 디버그 정보 |
| `trace` | 모든 상세 정보 |

---

## 문제 해결

| 문제 | 해결 |
|------|------|
| 연결 실패 | ID/비밀번호 확인, 네트워크/방화벽 점검 |
| 비밀번호 오류 | `sdfdesk --password <new>` 로 재설정 |
| 포트 충돌 | `netstat -ano | findstr :<port>` 확인 |
| NAT 문제 | UPnP 활성화, 릴레이 서버 사용 |

---

## 보안 권장사항

1. 12자 이상 강력한 비밀번호 사용
2. 명령줄 비밀번호 노출 주의 (가능하면 `--password`로 저장)
3. 신뢰할 수 있는 네트워크/VPN 사용
4. 필요한 포트만 포워딩

---

## 버전 이력

| 버전 | 변경사항 |
|------|----------|
| 2.1.8 | 피어 목록 조회 (`--list-peers`), 키보드 Modifier 지원 |
| 2.1.6 | Electron GUI, 마우스 제어, 연결 비밀번호 옵션 (`-P`) |
| 2.1.0 | 로컬 서버 모드, 화면 스트리밍 |

---

## 참고 자료

- [RustDesk](https://rustdesk.com)
- [RustDesk GitHub](https://github.com/rustdesk/rustdesk)
- [sdfdesk-server](../sdfdesk-server/)

---

## 버전 확인

```bash
sdfdesk --version
# sdfdesk 2.1.8
```
